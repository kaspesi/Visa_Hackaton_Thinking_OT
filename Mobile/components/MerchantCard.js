import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

export default () => {
	return (
		<View>
			<Text>This is the merchant card, should use to render inside of merchant list screen</Text>
		</View>
	);
};

const styles = StyleSheet.create({});
